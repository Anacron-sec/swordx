#include <stdlib.h>
#include <stdio.h>
#include "utils.h"

int main(int argc, char const *argv[])
{
    int zero = map_char('8');
    printf("Char a is mapped to int %d", zero);
    exit(EXIT_SUCCESS);
}
